# Hoid optimized models
This repo highlights the models where hoid generated kernels achieve superior performance compared to PyTorch. All of the models are loaded as pytorch modules with custom ops for kernels. 
